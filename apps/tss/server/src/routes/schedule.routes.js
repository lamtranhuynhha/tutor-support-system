import { Router } from "express";
// import {} from "../controllers/schedule.controller.js";

export const router = Router();

// DEFINE ROUTES HERE
// Example:
// router.put("/:id",updateSchedule); // query /:id?onwer=userId
// router.get("/:id",getSchedule); // query /:id?onwer=userId
// router.delete("/:id",deleteSchedule); // query /:id?onwer=userId
// router.get("/list",getAllSchedule);
// Ex: api/schedule/list?page=1&limit=10&sort=asc&onwer=userId....
// if query params not provided, use default values in controller (recommended)
